services:
  backend:
   build:
     context: .
     dockerfile: ./Dockerfile
   command: python transBharat/manage.py runserver 0.0.0.0:8000
   volumes:
    - .:/app:cached
   ports:
     - 8000:8000
   env_file:
    - ./.env